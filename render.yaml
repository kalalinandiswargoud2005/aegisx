services:
  - type: web
    name: aegisx-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    plan: free
    envVars:
      - key: PORT
        value: 8080
      - key: GEMINI_API_KEY
        sync: false
